# Contributing

1. Fork ve feature branch oluşturun.
2. Çalışmanızı `pre-commit` ile kontrol edin (`pre-commit install`).
3. Testleri çalıştırın: `python -m pytest -q`
4. PR açarken ilgili `work/applied` örneklerini ekleyin.

Small tasks: tamamlanma hedefi 3 iş günü.
