This folder contains example applied patches and short instructions for reviewers and implementers.

Usage:
- Inspect patch files in `work/applied/examples/`.
- Apply via `git apply <patch>` in a feature branch and run local tests.

Each patch is minimal and demonstrates the expected patch format for implementers.
