This folder is the canonical location for proposal files that `khamul` (PO) submits for `witch-king` review.

Place proposal markdown files (with frontmatter blocks) here before running `scripts/trigger_witchking_approval.ps1`.
